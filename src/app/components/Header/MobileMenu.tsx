'use client';
import React from "react";

function loadMobileMenu(){
    
}

const MobileMenu = () => {
    return (
        <a href="" onClick={loadMobileMenu} key={1000}><i className="fas fa-bars" ></i></a>
    );
}

export default MobileMenu
